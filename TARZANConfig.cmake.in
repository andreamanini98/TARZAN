@PACKAGE_INIT@


# Include macro for finding dependencies.
include(CMakeFindDependencyMacro)


# Find Boost library.
find_dependency(Boost 1.89.0 REQUIRED filesystem)


# Find Abseil libraries.
find_package(absl CONFIG REQUIRED)


# Import the targets.
include("${CMAKE_CURRENT_LIST_DIR}/TARZANTargets.cmake")


# Make sure all required components were found.
check_required_components(TARZAN)