create automaton taTA_0
{
    clocks
    {
        x, y, z;
    }

    actions
    {
        a, b, c;
    }

    locations
    {
        q0 <initial: true>,
        q1 <initial: false>,
        q2 <initial: true>,
        q3,
        q4 <initial:true>;
    }

    transitions
    {
        (q0, action, [(x, <, 4), (y, =, 5)], [x, y, z], q1),
        (q1, action2, [(x, >=, 4), (z, <=, 5)], [x, y, z], q2);
    }
}