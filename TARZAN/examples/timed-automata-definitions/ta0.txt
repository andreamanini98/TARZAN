create automaton taTA_0
{
    clocks
    {
        x, y, z;
    }

    actions
    {
        a, b, c;
    }

    locations
    {
        q0 <ini: true>,
        q1 <ini: false>,
        q2 <ini: false>,
        q3 <>,
        q4 <ini: false>;
    }

    transitions
    {
        (q0, action, [(x, =, 1), (y, =, 1)], [x], q1),
        (q1, action2, [(x, >, 2), (z, <, 4)], [x, z], q2);
    }
}