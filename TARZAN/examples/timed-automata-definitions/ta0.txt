create automaton taTA_0
{
    clocks
    {
        x, y, z;
    }

    actions
    {
        a, b, c;
    }

    locations
    {
        q0 <initial: true>,
        q1 <initial: false>,
        q2 <initial: false>,
        q3,
        q4 <initial:false>;
    }

    transitions
    {
        (q0, action, [(x, =, 1), (y, =, 1)], [x], q1),
        (q1, action2, [(x, >, 2), (z, <, 4)], [x, z], q2);
    }
}