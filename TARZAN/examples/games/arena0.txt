create arena arena_0
{
    clocks
    {
        x, y, z;
    }

    actions
    {
        a, b;
    }

    locations
    {
        q0 <player: c, <ini: T>>,
        q1 <player: e, <ini: F>>,
        q2 <player: c, <urg: T>>;
    }

    transitions
    {
        (q0, a, [(x, <=, 1), (y, ==, 2)], [x, y, z], q1),
        (q1, b, [(x, >=, 1), (z, <=, 1)], [z],       q2);
    }
}