create arena semantics
{
    clocks
    {
        x;
    }

    actions
    {
        a, b;
    }

    locations
    {
        q0 <player: e, <ini: T>>,
        A  <player: c, <ini: F>>,
        B  <player: c, <ini: F>>,
        C  <player: c, <ini: F>>;
    }

    transitions
    {
        (q0, b, [(x, <, 1)],            [x], A),
        (q0, a, [(x, ==, 1)],           [x], B),
        (q0, a, [(x, >, 1), (x, <, 2)], [x], C);
    }
}