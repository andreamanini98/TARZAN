create automaton Torch
{
    clocks {  }
    actions { a, release, take; }
    integers { L; }
    locations {
        one  <ini: F        >,
        L0   <ini: F, urg: T>,
        free <ini: T        >,
        two  <ini: F        >;
    }
    transitions {
        (free, take?, [], [], L0),
        (L0, a, [], [], one),
        (L0, take?, [], [], two),
        (one, release?, [], [], [L = 1 - L], free),
        (two, release?, [], [], one);
    }
}