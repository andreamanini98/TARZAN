create automaton P26
{
  clocks { x; }
  actions { begin, busy, cd26, end; }
  locations {
    sender_wait   <ini: T                     >,
    sender_transm <ini: F, inv: [(x, <=, 808)]>,
    sender_retry  <ini: F, inv: [(x, <, 52)]  >;
  }
  transitions {
    (sender_wait  , begin!, []            , [], sender_transm),
    (sender_wait  , cd26? , []            , [], sender_wait),
    (sender_wait  , cd26? , []            , [], sender_retry),
    (sender_wait  , busy? , []            , [], sender_retry),
    (sender_transm, end!  , [(x, ==, 808)], [], sender_wait),
    (sender_transm, cd26? , [(x, <, 52)]  , [], sender_retry),
    (sender_retry , begin!, [(x, <, 52)]  , [], sender_transm),
    (sender_retry , cd26? , [(x, <, 52)]  , [], sender_retry);
  }
}