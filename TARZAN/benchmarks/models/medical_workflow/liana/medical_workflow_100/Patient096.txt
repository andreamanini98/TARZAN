create automaton Patient096
{
    clocks { x, patientFlow; }
    actions { a, checkID, medicalCheck, patientArrivalDoc096, patientArrivalNurse096; }
    integers { docReady, nurseReady; }
    locations {
        Done                <ini: F                             >,
        WaitingMedicalCheck <ini: F, inv: [(x, <=, 9)]          >,
        Waiting             <ini: F, inv: [(x, <=, 9)]          >,
        Ready               <ini: T, inv: [(patientFlow, <=, 9)]>,
        L0                  <ini: F, urg: T                     >;
    }
    transitions {
        (L0, patientArrivalNurse096!, [], [], Waiting),
        (Ready, a, [(patientFlow, >=, 9)], [patientFlow], Ready),
        (WaitingMedicalCheck, medicalCheck?, [], [], Done),
        (Waiting, checkID?, [], [x], WaitingMedicalCheck),
        (Ready, patientArrivalDoc096!, [(patientFlow, >=, 9)], docReady == 1 && nurseReady == 1, [x, patientFlow], L0);
    }
}