create automaton fischer02
{
    clocks { x; }
    actions { a; }
    integers { id; }
    locations {
        wait <ini: F                   >,
        req  <ini: F, inv: [(x, <=, 2)]>,
        A    <ini: T                   >,
        cs   <ini: F                   >;
    }
    transitions {
        (A, a, [], id == 0, [x], req),
        (req, a, [(x, <=, 2)], [x], [id = 2], wait),
        (wait, a, [], id == 0, [x], req),
        (wait, a, [(x, >, 2)], id == 2, [], cs),
        (cs, a, [], [], [id = 0], A);
    }
}