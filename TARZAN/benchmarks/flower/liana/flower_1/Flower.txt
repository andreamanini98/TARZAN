create automaton Flower
{
    clocks { x1, y; }
    actions { a; }
    locations {
        Start <ini: T>,
        Goal  <ini: F>;
    }
    transitions {
        (Start, a, [(x1, ==, 1)], [x1], Start),
        (Start, a, [(x1, ==, 0), (y, >=, 1)], [], Goal);
    }
}